# Optional: point at a specific API (skips Vite dev proxy when set).
# Local API: VITE_PATRO_API_URL=http://127.0.0.1:8000
# Production API: VITE_PATRO_API_URL=https://84-235-248-118.sslip.io
#
# With no VITE_PATRO_API_URL, `npm run demo` proxies /patro, /holidays, etc. to the VM.
